{
  "name": "snk",
  "description": "Generates GitHub contribution animations with a custom RX-78-2 Gundam renderer",
  "version": "3.4.1",
  "private": true,
  "repository": "github:m-mohamed/tachikoma",
  "devDependencies": {
    "@types/bun": "1.3.4",
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "type": "tsc",
    "lint": "prettier -c '**/*.{ts,js,json,md,yml,yaml}' '!packages/*/dist/**' '!svg-only/dist/**'",
    "dev:demo": "( cd packages/demo ; npm run dev )",
    "build:demo": "( cd packages/demo ; npm run build )",
    "build:action": "( cd packages/action ; npm run build )"
  }
}
